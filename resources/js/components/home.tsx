import React from "react";

const Home = () => {
    let foo: string = 'Reactsss';
    const bar: string = 'TP';

    return (
        <h1>
            {foo} + {bar}
        </h1>
    )
};

export default Home;